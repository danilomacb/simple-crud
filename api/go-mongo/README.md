```bash
# If you don't set PATH environment variable already
export PATH=$PATH:/usr/local/go/bin

# Install the dependencies
go get go.mongodb.org/mongo-driver/mongo
go get github.com/gorilla/mux

# Run all files
go run .
```
